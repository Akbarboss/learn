{
  "rules": {
    "$learner": {
      ".read": "$learner === 'Akbar' || $learner === 'Abror' || $learner === 'Muhammadali' || $learner === 'Zilola'",
      ".write": "$learner === 'Akbar' || $learner === 'Abror' || $learner === 'Muhammadali' || $learner === 'Zilola'"
    }
  }
}
